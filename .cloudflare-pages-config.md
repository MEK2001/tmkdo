# Cloudflare Pages Configuration

Build command: `npm run build`
Build output directory: `out`
Root directory: `/`

Environment variables:
- NODE_VERSION: `18`

Note: These settings should be configured in the Cloudflare Pages dashboard under Settings > Builds & deployments.
