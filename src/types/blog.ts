// Frontend blog post type (compatible with existing components)
export interface BlogPost {
  slug: string;
  image: string;
  date: string;
  readTime: string;
  title: string;
  excerpt: string;
  category: string;
}
