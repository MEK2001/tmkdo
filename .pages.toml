# Cloudflare Pages Build Configuration

[build]
command = "npm run build"
publish = "out"

[build.environment]
NODE_VERSION = "22"
NEXT_PUBLIC_SANITY_PROJECT_ID = "yr3kaxmk"
NEXT_PUBLIC_SANITY_DATASET = "production"

# Production branch
[env.production]
NEXT_PUBLIC_SANITY_PROJECT_ID = "yr3kaxmk"
NEXT_PUBLIC_SANITY_DATASET = "production"
